/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"cdn.noitatnemucod.net",
			"assets.aceternity.com",
			"cdn.myanimelist.net",
			"image.tmdb.org",
		],
	},
};

module.exports = nextConfig;
